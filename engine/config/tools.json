[
  {
    "name": "shell",
    "description": "Execute a shell command in the agent's sandboxed workspace. Available tools: git, ls, cd, tar, find, xargs, grep, echo, zip, unzip.",
    "program": "/bin/bash",
    "args": ["-c", "{{command}}"],
    "parameters": {
      "command": { "type": "string", "description": "The shell command to execute" }
    },
    "required": ["command"],
    "timeout_secs": 30
  },
  {
    "name": "python",
    "description": "Execute Python code in the agent's sandboxed workspace",
    "program": "python3",
    "args": ["-c", "{{code}}"],
    "parameters": {
      "code": { "type": "string", "description": "Python code to execute" }
    },
    "required": ["code"],
    "timeout_secs": 30
  }
]
