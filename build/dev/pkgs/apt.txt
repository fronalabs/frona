# Extra packages layered on top of prod-apt.txt for the dev image: the Rust build
# toolchain. Prod copies release binaries from backend-builder, so it needs none of
# these; everything else the dev server needs at runtime comes from prod-apt.txt.
build-essential=12.9
# Faster incremental links for the very large debug server binary.
clang=1:14.0-55.7~deb12u1
lld=1:14.0-55.7~deb12u1
pkg-config=1.8.1-1
# OpenSSL headers for openssl-sys + libsqlite3-sys (SQLCipher, -DSQLITE_HAS_CODEC).
libssl-dev=3.0.20-1~deb12u2
# bindgen for native -sys crates (e.g. libsqlite3-sys and Signal deps).
libclang-dev=1:14.0-55.7~deb12u1
# presage's transitive proto build-scripts.
protobuf-compiler=3.21.12-3+deb12u1
